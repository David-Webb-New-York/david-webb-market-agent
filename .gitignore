node_modules/
probe-output/

# Deploy-time copy pages-deploy.yml packages from output/*.json -- not the
# source of truth, regenerated fresh on every deploy.
docs/data/

# Transient Slack payloads generated by analyze.js (--generate writes them,
# --notify consumes them in the same run; no need to track in git)
output/reports/*.slack.json
