{
  "url": "https://www.invaluable.com/search?keyword=david+webb",
  "proxies": false,
  "wait_ms": 12000,
  "note": "Re-probe after adding jsonShape summarizer to bb-probe.js. First probe already found the real endpoint: https://www.invaluable.com/catResults returns real lot data (lotTitle, lotNumber, estimateLow/High, currentBid, priceResult, houseName, saleType, dateTimeUTCUnix, objectID) wrapped in an Algolia-style results[0].hits[] response -- but the body was too large to see nbHits/page/nbPages pagination fields in the truncated log dump. bb-probe.js now computes a cheap structural summary (array lengths + pagination-looking numeric fields) for every candidate JSON response so this run should surface those numbers directly instead of guessing at Algolia's usual field names."
}
