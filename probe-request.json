{
  "url": "https://www.liveauctioneers.com/search/?keyword=david+webb",
  "proxies": false,
  "wait_ms": 12000,
  "note": "re-probe with undefined-token sanitization fix: window.__data extraction found the right 84686-byte boundary but JSON.parse failed on a bare `undefined` token (not valid JSON, only null is) -- now sanitized before parsing. Should finally yield the full parsed __data + matched lot-like objects."
}
