{
  "url": "https://www.invaluable.com/search?keyword=david+webb",
  "proxies": false,
  "wait_ms": 12000,
  "note": "Re-probe #2: jsonShape summarizer already confirmed catResults is a standard Algolia envelope: results[0].{hits[], nbHits:133, page:0, nbPages:2, hitsPerPage:96}. But every captured catResults URL was bare (https://www.invaluable.com/catResults, no query string) across 4 separate captures, all page:0 -- meaning pagination/query params must be in a POST body the probe wasn't capturing. browserbase.js + bb-probe.js now capture request method + postData for every JSON response, so this run should reveal the actual request shape (query text, page number encoding) needed to fetch page 1 of 2."
}
