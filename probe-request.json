{
  "url": "https://www.sothebys.com/en/search?query=david%20webb",
  "proxies": true,
  "wait_ms": 10000,
  "note": "Task #5: first probe of Sotheby's this session. import-all.js currently lists it as 'SPA + API, GET redirects to JS app' from an earlier (pre-this-session) investigation, never re-verified with the current bb-probe.js tooling (inline window-var extraction, method+postData capture, jsonShape summarizer, tiny-HTML full dump). URL is an educated guess based on common site conventions -- probing to find the real search API/URL pattern rather than assuming this is right."
}
