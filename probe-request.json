{
  "url": "https://www.ha.com/search?keyword=david+webb",
  "proxies": true,
  "wait_ms": 10000,
  "note": "Re-probe: first Heritage attempt returned only 3965 bytes titled just 'ha.com', zero signals, zero XHRs, zero embedded state -- looks like a bot-block interstitial or a shell that never initialized, unlike Bonhams' first attempt (real 638KB page, wrong query param but a real API found). bb-probe.js now prints the full raw HTML when it's under 10KB (too small to be a real page, but small enough to just show directly) so this run should reveal exactly what's actually being served instead of guessing whether it's a CAPTCHA, a redirect, or something else."
}
