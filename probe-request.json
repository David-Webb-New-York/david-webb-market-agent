{
  "url": "https://www.liveauctioneers.com/search/?keyword=david+webb",
  "proxies": false,
  "wait_ms": 12000,
  "note": "re-probe to confirm listing_url pattern: previous run found 8 real lot-like objects (window.__data.itemSummary.byId.<itemId>) with itemId/slug fields but the deep-dive never checked whether an <a href> in the rendered HTML actually contains those exact itemId/slug values. print-probe-findings.js now searches for that directly, so this run should tell us the real item-page URL pattern instead of guessing one from general site knowledge."
}
